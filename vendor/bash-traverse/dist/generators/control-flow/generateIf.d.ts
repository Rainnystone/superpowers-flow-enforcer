import { IfStatement } from '../../types';
/**
 * If statement generator
 * If statements commonly need explicit spacing: "if [condition]; then"
 */
export declare function generateIfStatement(ifStatement: IfStatement): string;
//# sourceMappingURL=generateIf.d.ts.map